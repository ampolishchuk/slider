import SliderController from "./SliderController";

const sliderController = new SliderController();

describe("Testing SliderController.ts", () => {
  it("is defined", () => {
    expect(sliderController).toBeDefined();
  });
});
